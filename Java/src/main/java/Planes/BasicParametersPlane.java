package Planes;

public class BasicParametersPlane extends Plane {
    public BasicParametersPlane(String model, int maxSpeed, int maxFlightDistance, int maxLoadCapacity) {
        super(model, maxSpeed, maxFlightDistance, maxLoadCapacity);
    }
}
